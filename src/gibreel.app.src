{application, gibreel, [
	{description, "Gibreel Distributed Cache System"},
	{vsn, "0.9.0"},
	{modules, [
	]},
	{registered, [
		gibreel,
		g_cache_sup
	]},
	{applications, [
		kernel, 
		stdlib, 
		columbo,
		cclock,
		async
	]},
	{mod, {gibreel_app, []}},
	{env, []}
]}.
